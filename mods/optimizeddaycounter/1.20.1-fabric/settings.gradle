pluginManagement {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
		gradlePluginPortal()
	}
}

// JAR archive name is set via base { archivesName = ... } in build.gradle
rootProject.name = 'mod-build'
